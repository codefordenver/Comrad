const VERSION = 'v1';

export const ROOT_ALBUMS_URL = `/${VERSION}/albums`;
export const ROOT_ARTISTS_URL = `/${VERSION}/artists`;
export const ROOT_AUTH_URL = `/${VERSION}/auth`;
export const ROOT_PERMISSION_URL = `/${VERSION}/permissions`;
export const ROOT_RESOURCES_URL = `/${VERSION}/resources`;
export const ROOT_SHOWS_URL = `/${VERSION}/shows`;
export const ROOT_TRACKS_URL = `/${VERSION}/tracks`;
export const ROOT_USERS_URL = `/${VERSION}/users`;
